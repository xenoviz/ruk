# Ruk's error strings are part of its public human and JSON output, which the
# frozen conformance golden pins, so the capitalization style check does not
# apply. All other default checks stay enabled.
checks = ["inherit", "-ST1005"]
